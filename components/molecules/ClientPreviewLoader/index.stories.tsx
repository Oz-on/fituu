import ClientPreviewLoader from "./";

export default {
  title: "Components/Molecules/ClientPreviewLoader",
  component: ClientPreviewLoader,
}

const Template = (args) => <ClientPreviewLoader {...args} />;

export const Default = Template.bind({});
