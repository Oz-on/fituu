import EmptyInstructorPane from ".";

export default {
  title: "Components/Organisms/EmptyInstructorPane",
  component: EmptyInstructorPane,
};

const Template = (args) => <EmptyInstructorPane {...args} />;

export const Default = Template.bind({});